# RFC: Schema-first HTTP contracts

Status: proposed.

Define runtime validation for new and legacy HTTP DTOs, a shared error shape,
and generation and compatibility rules. Until accepted, new changes reuse
`web/shared` types without attempting a repository-wide DTO rewrite.
