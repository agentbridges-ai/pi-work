# Credential compromise

Stop the affected provider or MCP, revoke external credentials, freeze the
affected sessions, and preserve a redacted request timeline. Never copy the
secret into an issue or log. Use Private Vulnerability Reporting to record
scope, rotation time, and verification. Review Git history, `.env` permissions,
the bootstrap channel, and child-process environments before reopening service.
