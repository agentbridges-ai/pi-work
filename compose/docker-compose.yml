name: piwork

services:
  caddy:
    image: ${PIWORK_CADDY_IMAGE:-caddy:2.10.2}
    restart: unless-stopped
    ports:
      - "${PIWORK_BIND_ADDRESS:-127.0.0.1}:${PIWORK_HTTP_PORT:-3457}:80"
    volumes:
      - ./compose/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - edge
    depends_on:
      web:
        condition: service_started

  web:
    entrypoint: ["/workspace/compose/web-entrypoint.sh"]
    restart: unless-stopped
    environment:
      NODE_ENV: ${NODE_ENV:-development}
      HOST: 0.0.0.0
      PORT: 3457
      VITE_PORT: 3458
      PIWORK_SERVE_FRONTEND: "0"
      PIWORK_DATA_ROOT: /var/lib/piwork/data
      PIWORK_POSTGRES_APP_USER: ${PIWORK_POSTGRES_APP_USER:-piwork_web}
      PIWORK_POSTGRES_APP_PASSWORD_FILE: /run/secrets/piwork-postgres-app-password
      POSTGRES_DB: ${POSTGRES_DB:-piwork}
      BETTER_AUTH_URL: ${BETTER_AUTH_URL:-http://127.0.0.1:3457}
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?run selfhost init first}
      PIWORK_RUNTIME_MODE: compose
      PIWORK_RUNTIME_SOCKET: /run/piwork-runtime/runtime.sock
      PIWORK_RUNTIME_CONTROL_KEY_FILE: /run/secrets/piwork-runtime-control
      PIWORK_RUNTIME_SECURITY_GATE: verified
      PIWORK_REQUIRE_SESSION_SANDBOX: "1"
      PIWORK_SESSION_SANDBOX: srt
      PIWORK_INTERNAL_PROXY_ONLY: "1"
    secrets:
      - source: piwork-runtime-control
        target: piwork-runtime-control
        uid: "65532"
        gid: "65532"
        mode: 0400
      - source: piwork-postgres-app-password
        target: piwork-postgres-app-password
        uid: "65532"
        gid: "65532"
        mode: 0400
    volumes:
      - piwork-data:/var/lib/piwork/data
      - runtime-socket:/run/piwork-runtime:ro
    networks:
      - edge
      - backend
    depends_on:
      postgres:
        condition: service_healthy
      runtime:
        condition: service_started

  runtime:
    restart: unless-stopped
    user: "65532:65532"
    read_only: true
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
      - seccomp:./compose/runtime/seccomp.json
    pids_limit: 256
    mem_limit: ${PIWORK_RUNTIME_MEMORY_LIMIT:-2g}
    tmpfs:
      - /tmp:rw,noexec,nosuid,nodev,size=256m
      - /run:rw,nosuid,nodev,size=32m
    environment:
      NODE_ENV: production
      PIWORK_DATA_ROOT: /var/lib/piwork/data
      PIWORK_RUNTIME_SOCKET: /run/piwork-runtime/runtime.sock
      PIWORK_RUNTIME_CONTROL_KEY_FILE: /run/secrets/piwork-runtime-control
      PIWORK_RUNTIME_DEPLOYMENT_MODE: compose-nested
      PIWORK_RUNTIME_SECURITY_GATE: verified
      PIWORK_RUNTIME_SECURITY_MARKER: /run/secrets/runtime-security.marker
    secrets:
      - source: piwork-runtime-control
        target: piwork-runtime-control
        uid: "65532"
        gid: "65532"
        mode: 0400
      - source: runtime-security-marker
        target: runtime-security.marker
        uid: "65532"
        gid: "65532"
        mode: 0400
    volumes:
      - piwork-data:/var/lib/piwork/data
      - runtime-socket:/run/piwork-runtime
    networks:
      - backend

  postgres:
    image: ${PIWORK_POSTGRES_IMAGE:-postgres:16.4}
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-piwork}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-piwork}
      POSTGRES_DB: ${POSTGRES_DB:-piwork}
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 20

  migrate:
    profiles: [migrate]
    entrypoint: ["/workspace/compose/migrate-entrypoint.sh"]
    restart: "no"
    environment:
      NODE_ENV: production
      PIWORK_DATA_ROOT: /var/lib/piwork/data
      DATABASE_URL: postgres://${POSTGRES_USER:-piwork}:${POSTGRES_PASSWORD:-piwork}@postgres:5432/${POSTGRES_DB:-piwork}
      PIWORK_POSTGRES_APP_USER: ${PIWORK_POSTGRES_APP_USER:-piwork_web}
      PIWORK_POSTGRES_APP_PASSWORD_FILE: /run/secrets/piwork-postgres-app-password
      BETTER_AUTH_SECRET: ${BETTER_AUTH_SECRET:?run selfhost init first}
    volumes:
      - piwork-data:/var/lib/piwork/data
    networks:
      - backend
    secrets:
      - source: piwork-postgres-app-password
        target: piwork-postgres-app-password
        uid: "65532"
        gid: "65532"
        mode: 0400
    depends_on:
      postgres:
        condition: service_healthy

networks:
  edge:
    driver: bridge
  backend:
    internal: true

volumes:
  piwork-data:
  postgres-data:
  runtime-socket:
  caddy-data:
  caddy-config:

secrets:
  piwork-runtime-control:
    file: ${PIWORK_RUNTIME_CONTROL_KEY_FILE_HOST:-.runtime/selfhost/runtime-control.key}
  runtime-security-marker:
    file: ${PIWORK_RUNTIME_SECURITY_MARKER_FILE:-.runtime/selfhost/runtime-security.marker}
  piwork-postgres-app-password:
    file: ${PIWORK_POSTGRES_APP_PASSWORD_FILE_HOST:-.runtime/selfhost/postgres-app.password}
