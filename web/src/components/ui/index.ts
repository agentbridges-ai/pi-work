// Compatibility boundary for existing Web imports. The implementation lives in
// the shared workspace package so every Piwork application consumes one API.
export * from "../../../../packages/ui/src/index.js";
